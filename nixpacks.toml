[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = ["mvn package -DskipTests -pl . -am"]

[start]
cmd = "java -Dspring.profiles.active=prod -jar target/CabBookingManagementSystem-0.0.1-SNAPSHOT.jar"
